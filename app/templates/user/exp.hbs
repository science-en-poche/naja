<div class="row-fluid">
  <div class="offset2 span8">
    <h1>{{#linkTo 'user' owner}}{{owner.id}}{{/linkTo}} / {{name}}</h1>
    <h2><small>{{description}}</small></h2>
  </div>
</div>
<div class="row-fluid">
  {{#if currentUserHasAccess}}
    <div class="offset2 span4">
      <h2>Results</h2>
      <hr>
    </div>
    <div class="span4">
      <h2>Graph</h2>
      <hr>
    </div>
  {{else}}
    <div class="offset2 span8">
      <h2>Oops!</h2>
      <hr>
      <p>You don't have acces to this page... did you forget to log in? Or you're not working with {{#linkTo 'user' owner}}{{owner.id}}{{/linkTo}} on this experiment.</p>
    </div>
  {{/if}}
</div>
